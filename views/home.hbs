<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>header hbs file inject Website</title>
</head>
  <body>
    {{> header}}
    <p>{{name}}</p>
    <p>{{likes}}</p>
    <p>{{screamIt 'Hello'}}</p>

    {{> footer}}
  </body>
</html>
